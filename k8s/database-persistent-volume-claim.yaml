apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-persistent-volume-claim
spec:
  accessModes:
    - ReadWriteOnce  # can be RW, but only by a single Node
  resources:
    requests:
      storage: 512MB
